## Objective
 A client that opens a socket, sets it to non-blocking mode (`fcntl` or `setsockopt`), attempts a connect, and handles the `EAGAIN` error gracefully
## Status
Ended using gemini. Code wasnt written by me here.

## Files
- **src/client.cpp** : Contains the client code 
